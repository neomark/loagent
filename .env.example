# LLM 설정 (OpenAI 호환 API)
LLM_BASE_URL=URL
LLM_API_KEY=TEST
LLM_MODEL=bcgpt-27b

# 임베딩 설정 (동일 서버)
EMBEDDING_MODEL=bge-m3

# OpenKB용 LiteLLM 설정 (OpenKB 내부에서 사용)
OPENAI_API_BASE=URL
OPENAI_API_KEY=TEST

# SSL 검증 우회 설정 (Windows 환경 고려)
CURL_CA_BUNDLE=""
HTTPX_VERIFY=False
